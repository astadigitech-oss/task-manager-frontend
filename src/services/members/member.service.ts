// import { apiClient } from "@/lib/api/client";
// import { API_ENDPOINTS } from "@/lib/api/endpoints";
// import { MemberListResponse, MemberResponse } from "@/types/api/team.api";

// export const membersService = {
//     /**
//      * Get all members inside a workspace
//      */
//     getAllMembers: async (workspace_id: number): Promise<MemberListResponse> => {
//         const url = API_ENDPOINTS.WORKSPACE.MEMBERS.LIST(workspace_id);
//         const response = await apiClient.get(url);
//         return response.data;
//     },
// };
